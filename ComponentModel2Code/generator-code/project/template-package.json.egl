{
    "private":"true",
    "name":"root",
    "license": "(EPL-2.0 OR GPL-2.0 WITH Classpath-exception-2.0 OR MIT)",
    "scripts": {
        "prepare":"yarn build",
        "build":"yarn build:server && yarn build:client",
        "build:client":"yarn --cwd [%=_clientPath%] install",
        "build:server":"cd [%=_serverPath%] && mvn --batch-mode clean verify"
    }
}